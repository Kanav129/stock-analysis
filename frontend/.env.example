# Leave unset (or use /api) for local Vite proxy → http://localhost:8001
# Production (Vercel): full Render API origin, no trailing slash
# VITE_API_BASE_URL=https://stocks-insights-api.onrender.com
VITE_API_BASE_URL=
